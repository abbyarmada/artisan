[run]
include =
    artisan/*
omit =
    artisan/compat.py
    artisan/worker/ssh

[report]
exclude_lines =
    except ImportError:
    pass
    import
    raise
    .* # Platform-specific.*
    .*:.* # Python \d.*
    .* # Skip coverage.*
